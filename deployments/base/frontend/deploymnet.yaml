apiVersion: apps/v1
kind: Deployment
metadata:
  name: voicera-frontend
  namespace: voicera-frontend
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  selector:
    matchLabels:
      app: voicera-frontend
  template:
    metadata:
      labels:
        app: voicera-frontend
    spec:
      nodeSelector:
        app: voicera
      containers:
        - name: voicera-frontend
          image: registryharbor.bhashini.co.in/bhashini/voicera/voicera-frontend-prod
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: "1"
              memory: "4Gi"
            requests:
              cpu: "200m"
              memory: "1Gi"
          envFrom:
            - secretRef:
                name: voicera-backend-secret
